import React from 'react';
import SillyCounter from './containers/sillycounter';

export default class App extends React.Component {
    render(){
        return(
            <div>
                <SillyCounter />
            </div>
        )
    }
}